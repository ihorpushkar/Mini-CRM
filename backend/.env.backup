# Server
PORT=3000
NODE_ENV=development

# Database (SQLite for local dev)
DATABASE_URL="file:./dev.db"

# JWT — minimum 32 characters
JWT_SECRET=dev-local-jwt-secret-key-min-32-chars!!
JWT_EXPIRES_IN=24h
